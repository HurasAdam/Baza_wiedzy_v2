export interface IConfig {
  NODE_ENV: string;
  MONGO_URI: string;
  PORT: string;
  JWT_SECRET: string;
  APP_ORIGIN: string;
  JWT_REFRESH_SECRET: string;
  SOCKET_PORT: string;
  EMAIL_SENDER: string;
}
