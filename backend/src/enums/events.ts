// eslint-disable-next-line import/prefer-default-export
export enum EEventType {
  Created = 'created',
  Updated = 'updated',
  Trashed = 'trashed',
  Restored = 'restored',
  verified = 'verified',
  Unverified = 'unverified',
}
