const enum EventType {
    Created = "created",
    Updated = "updated",
    Trashed = "trashed",
    Restored = "restored",
    verified = "verified",
    Unverified = "unverified",
}

export default EventType;
